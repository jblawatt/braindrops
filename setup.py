# coding: utf-8

from setuptools import setup


if __name__ == "__main__":
    setup(
        name="braindrops",
        version="1.0.0",
        install_requires=[
            "termcolor", "bottle", "tinydb"
        ]
    )
